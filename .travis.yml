sudo: required

services:
  - docker

before_install:
  - env | sort
  - docker pull obolibrary/odkfull

before_script:
  - cd src/ontology

script:
  - env | sort
  - ./run.sh make monarch-jenkins

# whitelist
branches:
  only:
    - master
    - test-travis
    - jenkins-setup

### UNCOMMENT THIS AND ADD YOUR DETAILS:
#notifications:
#  email:
#    - YOURNAME@YOU.ORG
